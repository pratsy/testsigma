OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o
# Only set this if port 8000 is already in use on your machine and you're
# running uvicorn on a different port instead (see README).
# APP_BASE_URL=http://127.0.0.1:8000
